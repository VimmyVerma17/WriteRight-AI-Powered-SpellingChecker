WriteRight — AI Spelling Checker

WriteRight is a minimal and fast Flask-based web application that corrects spelling and grammar with high accuracy. It supports both plain text and PDF uploads, detects mistakes instantly, and returns a clean corrected version for users. The app uses intelligent rule-based logic along with LanguageTool to ensure reliable corrections.

